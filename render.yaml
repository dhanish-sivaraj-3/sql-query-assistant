services:
  - type: web
    name: sql-query-assistant
    env: python
    plan: free
    buildCommand: |
      pip install -r requirements.txt
      # List files to verify ca.pem is present
      echo "📁 Files in repository:"
      ls -la
      echo "📁 Checking for ca.pem:"
      if [ -f "ca.pem" ]; then
        echo "✅ ca.pem found in root directory"
        cat ca.pem | head -2
      else
        echo "❌ ca.pem not found!"
      fi
    startCommand: gunicorn app:app
    envVars:
      - key: PORT
        value: 10000
      - key: PYTHON_VERSION
        value: 3.11.0
